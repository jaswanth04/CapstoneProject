#------to create mysql Docker -----------------
docker network create mysqlnet

docker run --rm -d -v mysql:/var/lib/mysql \
  -v mysql_config:/etc/mysql -p 3306:3306 \
  --network mysqlnet \
  --name mysqldb \
  -e MYSQL_ROOT_PASSWORD=p@ssw0rd1 \
  mysql


#-------------to get into docker and into db

 docker exec -ti mysqldb mysql -u root -p


#-----------DB Commands----------
use sys;

create table news(title varchar(1000), date varchar(1000) ,summary text,topic varchar(255),source varchar(1000) );

#-------------------------------------------read news --------
#Start kafka docker 


#*) Execute python3  kafkatosql.py  --> ( job to consume kafka messages and load into mysql DB) 
#1) Execute python3  news3.py --> ( RSS feed news)
#2) Execute pythnn3  news.py --> ( redampi feed news)


